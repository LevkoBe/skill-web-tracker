export const DEFAULT_SETTINGS = {
  connectionRange: 150,
  pointDriftRadius: 2,
  pointDriftSpeed: 1,
  maxProximityConnections: 3,
  timerActiveByDefault: false,
  durationScaleFactor: 1,
  showClusterLabels: true,
  showNoteLabels: true,
  breathingStrength: 0.3,
  gameMode: "gradual",
  unlockThreshold: 5,
};

export const MAX_HISTORY = 50;

export const DEFAULT_DIAGRAM_THEME_ID = "craft";

export const SUPPORTED_LOCALES = ["en", "uk"];
